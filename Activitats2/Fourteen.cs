using System;

/*namespace Activitats2
{
    
    public class Fourteen
    {
        public static int Max(int num, int num2)
        {
            
            return num > num2 ?  num :  num2;

        }
        public static int Min(int num, int num2)
        {
            return num == Max(num, num2) ? num2 : num;
        }
        public static void Main()
        {

            const string MsgOne = "Write two numbers please";
            const string MsgError = "Wrong Format";
            const string MsgEnd = "The max is : {0} and de min is: {1}";
            Console.WriteLine(MsgOne);
            try
            {
                int num = Convert.ToInt32(Console.ReadLine());
                int num2 = Convert.ToInt32(Console.ReadLine());

                Console.WriteLine(MsgEnd, Max(num, num2), Min(num, num2));
            }
            catch (FormatException)
            {
                Console.WriteLine(MsgError);
            }
        }
    }
}
*/