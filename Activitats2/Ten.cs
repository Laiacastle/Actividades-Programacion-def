using System;


/*namespace Activitats2
{
    class Ten
    {
        public static bool YearLeap(int year)
        {
            return year % 4 == 0;
        }
        public static void Main()
        {
            const string MsgOne = "Write a year";
            const string MsgError = "Wrong Format";
            const string MsgEnd = "The year is: {0}leap";

            try
            {
                Console.WriteLine(MsgOne);
                int year = Convert.ToInt32(Console.ReadLine());
                if (YearLeap(year))
                {
                    Console.WriteLine(MsgEnd, " ");
                }
                else
                {
                    Console.WriteLine(MsgEnd, " not");
                }
            }
            catch (Exception)
            { 
                Console.WriteLine(MsgError);
            }

        }
    }
}
*/